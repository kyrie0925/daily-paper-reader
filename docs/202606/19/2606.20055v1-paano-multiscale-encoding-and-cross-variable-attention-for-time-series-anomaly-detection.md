---
title: "PaAno+: Multiscale Encoding and Cross-Variable Attention for Time Series Anomaly Detection"
title_zh: PaAno+：用于时间序列异常检测的多尺度编码与跨变量注意力
authors: "Youji Zhu, Hongbing Wang, Wenchao Liu, Xiaodong Liu, Xiangguang Xiong"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20055v1"
tags: ["query:anomaly-id"]
score: 9.0
evidence: 轻量级时间序列异常检测模型，采用多尺度编码和跨变量注意力
tldr: 时间序列异常检测在工业与医疗监控中价值显著，但现有方法或计算开销大，或特征提取不足。本文提出轻量级PaAno+模型，采用多尺度卷积提取层次特征，通过跨尺度自适应注意力与残差连接稳定表示学习。引入跨变量融合注意力显式建模变量关联，并设计基于补丁窗口排序的预任务与三元组损失优化嵌入空间。在TSB-AD基准上，PaAno+在单变量和多变量任务中均取得SOTA检测精度，VUS-PR等指标显著提升，且计算高效可部署于资源受限终端。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20055v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1697, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20055v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1550, \"height\": 384, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20055v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1546, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20055v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1706, \"height\": 405, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20055v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1707, \"height\": 399, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20055v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1311, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20055v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1326, \"height\": 192, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20055v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1287, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20055v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1743, \"height\": 1005, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20055v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1685, \"height\": 848, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20055v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1670, \"height\": 477, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20055v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1713, \"height\": 441, \"label\": \"Table\"}]"
motivation: 现有Transformer和大模型方案计算开销大，轻量方法特征提取不充分且难以建模多变量依赖。
method: 构建多尺度卷积主干提取层次时序特征，经跨尺度自适应注意力与残差连接优化；嵌入跨变量融合注意力模块；设计补丁窗口排序预任务和三元组损失。
result: 在TSB-AD基准上，PaAno+在单变量和多变量任务中VUS-PR等指标达到SOTA，检测精度显著提升。
conclusion: PaAno+是一种轻量高效的时间序列异常检测模型，兼具高精度与低计算开销，适合实时边缘部署。
---

## 摘要
时间序列异常检测在工业与医疗监控及其他关键领域具有重要的实用价值。当前基于Transformer和大模型的检测方法计算开销过大，而现有的轻量级替代方案受限于特征提取不充分以及对多变量间依赖关系建模不足。为缓解上述缺陷，本研究在面向补丁的表征学习范式下，开发了一种轻量级高效异常检测模型PaAno。在编码器模块中，利用具有不同感受野的卷积核构建多尺度特征提取主干，以捕获层次化时间特征；随后通过跨尺度自适应注意力聚合，并结合残差连接优化，进一步稳定特征表示学习。嵌入跨变量融合注意力模块以显式表征变量间相关性，使模型能够在复杂运行条件下识别异常模式。此外，定制了一种基于时间补丁窗口排序的新型前置任务，以揭示时间序列的内在结构属性，并利用三元组损失优化补丁嵌入空间以增强特征区分度。在TSB-AD基准上的大量实验表明，所提出的PaAno在单变量和多变量任务上均实现了最先进的检测精度，在包括VUS-PR在内的评估指标上相比原始PaAno取得了显著性能提升。依托紧凑的网络设计，该模型实现了良好的计算效率，可部署于资源受限终端进行实时异常推理。

## Abstract
Time-series anomaly detection has significant practical value for industrial and medical monitoring, as well as other critical domains. Current Transformer- and large-model-based detection approaches incur excessive computational overhead, while existing lightweight alternatives are constrained by insufficient feature extraction and inadequate modeling of dependencies across multivariate variables. To mitigate the above drawbacks, this study develops a lightweight, efficient anomaly detection model, dubbed PaAno, within the patch-oriented representation learning paradigm. In the encoder module, a multiscale feature-extraction backbone is constructed using convolutional kernels with differentiated receptive fields to capture hierarchical temporal characteristics; subsequent cross-scale adaptive attention aggregation, combined with residual connection optimization, further stabilizes feature representation learning. A cross-variable fusion attention module is embedded to explicitly characterize inter-variable correlations, empowering the model to identify anomalous patterns amid intricate operational conditions. Moreover, a novel pretext task based on temporal patch-window sorting is customized to uncover intrinsic structural properties of time series, and triplet loss is leveraged to optimize the patch embedding space for enhanced feature discrimination. Extensive experiments on the TSB-AD benchmark demonstrate that the proposed PaAno achieves state-of-the-art detection accuracy on both univariate and multivariate tasks, yielding significant performance gains across evaluation metrics, including VUS-PR, relative to the original PaAno. Leveraging a compact network design, the presented model achieves favorable computational efficiency, enabling deployment on resource-limited terminals for real-time anomaly inference.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机与背景）
- **研究动机**：时间序列异常检测在工业监控、医疗诊断等关键领域具有重要价值。当前的主流方法分为三类：  
  - 传统统计与机器学习方法（如LOF、IForest）轻量但无法捕获复杂非线性局部特征。  
  - 深度神经网络方法（如LSTM、CNN）能捕获长程依赖，但难以建模跨变量关联，且超参数敏感。  
  - Transformer和大预测练模型（如AnomalyTransformer、MOMENT）虽全局建模能力强，但参数量巨大（数亿级），计算和内存开销过高，不适合资源受限的边缘终端实时部署。  
- **核心问题**：现有轻量级方法（如PaAno）存在三方面缺陷：  
  1. 固定感受野的单尺度卷积无法同时适应点状突变和长序列持续异常；  
  2. 独立编码多变量通道，忽略变量间相关性，削弱交互型异常检测能力；  
  3. 辅助前置任务仅约束补丁内时间连续性，监督信息不足，限制时序依赖建模深度。  
- **论文整体含义**：提出改进的轻量级模型PaAno+，通过多尺度编码、跨变量注意力和窗口排序前置任务，在保持低计算开销的同时显著提升单变量和多变量异常检测精度，实现“轻量”与“高准确率”的平衡。

### 2. 方法论：核心思想、关键技术细节与公式/算法流程
- **核心思想**：基于补丁（patch）的表征学习范式，构建判别性嵌入空间：正常时序模式形成紧凑特征簇，异常模式偏离该分布。流程包括三个阶段：补丁提取与预处理 → 编码器训练 → 异常推理。
- **关键技术细节**：
  - **多尺度时序编码器**：三分支并行卷积（核大小3、7、15），分别捕获短、中、长程时序特征。每个分支依次执行卷积、批归一化、ReLU、自适应平均池化，输出统一维度特征。  
    - **跨尺度自适应注意力融合**：将三分支输出沿特征维拼接，通过全连接层计算每尺度的注意力权重 \(W_{\text{attn},i}\)，加权得到融合特征 \(X_{\text{fused}} = \sum_{i=1}^3 W_{\text{attn},i} \cdot X_{\text{scale},i}\)。  
    - **残差连接**：每个分支添加线性投影的残差 \(X_{\text{final}} = X_{\text{fused}} + \gamma \cdot X_{\text{residual}}\)，\(\gamma\) 为可学习参数，稳定梯度传播。
  - **跨变量融合注意力**：仅用于多变量输入。将输入特征 \(X \in \mathbb{R}^{B \times D \times T}\) 经线性投影后计算 Q、K、V，沿变量维度执行多头自注意力（2头，dropout=0.1），得到注意力权重矩阵 \(A = \text{Softmax}(QK^\top / \sqrt{T})\)，加权融合 \(X_{\text{attn}} = AV\)；注意后与原始投影特征拼接，再经全连接层输出。
  - **窗口排序前置任务**：从连续补丁序列中随机选取 \(T\) 个连续补丁（单变量 \(T=2\)，多变量 \(T=5\)），随机打乱顺序，构造排序头（两层MLP, 512隐藏维度）预测逆排列 \(\pi^{-1}\)。损失函数为交叉熵形式（式13），仅在前20个训练epoch生效，权重 \(\lambda\) 从1线性衰减到0。
  - **联合损失函数**：总损失 = 三元组损失 + \(\lambda\) × 排序损失。  
    - **三元组损失**：锚点补丁 \(p_i\)，正样本 \(p_i^+\)（随机偏移 ±r 步），负样本 \(p_i^-\)（批次内余弦距离最远的补丁），margin \(\delta=0.5\)，如式(15)。
  - **异常推理**：训练后，用K-means压缩正常补丁嵌入记忆库（保留10%质心），测试时计算每个测试补丁与k个最近邻质心的平均余弦距离，最终点级得分为覆盖该时间戳的所有补丁得分的平均值。
- **公式与算法流程**：文中给出了主要的损失函数和注意力机制公式，算法流程以图2-4示意，通过文字描述了三阶段处理。

### 3. 实验设计：数据集、基准与对比方法
- **数据集**：采用TSB-AD基准（[5]），包含两个子集：  
  - **TSB-AD-U**（单变量）：350个测试序列，平均长度51,886.7步，异常比例4.5%。  
  - **TSB-AD-M**（多变量）：180个测试序列，平均长度108,826.7步，异常比例5.0%。  
  训练集仅含正常样本，测试集有精确异常标注；该基准消除了不正确标注、时间位置偏差、上下文不足、异常比例过高等常见缺陷，并提供公平一致的评估协议（不使用点调整）。
- **对比方法**涵盖三大类（共约20+种）：  
  - 统计/机器学习：SAND、DLinear、NLinear等。  
  - 深度网络：TimesNet、FITS、DADA、KAN-AD、CrossAD、CATCH等。  
  - Transformer/大模型：TranAD、AnomalyTransformer、DCdetector、PatchTST、iTransformer、MOMENT（微调/零样本）、TimesFM、Chronos、OFA、Lag-Llama等。
- **评估指标**：6个指标，分为区间级（VUS-PR、VUS-ROC、Range-F1）和点级（AUC-PR、AUC-ROC、Point-F1），主指标为VUS-PR；所有指标均不使用点调整操作，防止评估偏差。

### 4. 资源与算力
- **GPU型号与数量**：所有实验在单张 NVIDIA RTX 4090 GPU 上完成（文中明确说明）。未提及多卡或多机训练。
- **训练配置**：优化器Adam，batch size 512，学习率余弦退火从 \(1\times10^{-3}\) 到 \(1\times10^{-4}\)，总epoch 200。排序损失权重 \(\lambda\) 在前20个epoch线性衰减至0。
- **模型参数**：单变量1.1M，多变量1.5M；推理时间（处理整个评估集？）单变量12.9秒，多变量23.5秒（表4、5中Time列，单位为秒）。未给出详细训练时长，但基于参数规模和epoch数可推断训练耗时适中。
- **其他**：记忆库压缩比率默认10%，近邻数k=3。

### 5. 实验数量与充分性
- **实验组数**：非常丰富，包括：
  - 主实验结果：表4（单变量）和表5（多变量），各对比约15-20种方法。
  - 消融实验（表6）：针对多尺度（w/o MultiScale, w/o dual-scale）、跨变量注意力（w/o Attention）、排序预任务（w/o pretext_loss）共5个变体在双数据集上测试。
  - 超参数敏感性分析：  
    - 补丁大小（表3，32/64/96/128）。  
    - 卷积核组合（表7，4组）。  
    - 窗口长度T（图5，2/3/5/7）。  
    - 记忆压缩比率与k值（图4，1%–10%，k=1–5）。  
    - 排序损失权重\(\lambda\)（文本提及0.1/0.5/1/2）。
- **充分性评价**：实验设计客观公平，采用标准化基准TSB-AD（统一训练/测试分割，不含点调整），对比了多个技术流派的最新方法；消融和敏感性分析全面验证了各组件贡献及鲁棒性。多变量提升幅度不如单变量显著，但整体充分。

### 6. 主要结论与发现
- **单变量任务**：PaAno+ 在所有6个指标上取得最优（VUS-PR=0.55，VUS-ROC=0.90，Range-F1=0.52，AUC-PR=0.49，AUC-ROC=0.87，Point-F1=0.54），显著优于PaAno（VUS-PR=0.51）及其他基线。
- **多变量任务**：PaAno+ 在VUS-ROC（0.80）和AUC-ROC（0.77）上超越PaAno（0.79/0.76），其他指标与PaAno持平或略高（VUS-PR=0.42，PaAno=0.43），仍远超其他Transformer/大模型（如CATCH VUS-PR=0.30）。
- **消融贡献**：
  - 多尺度编码（三核优于双核与非多尺度）在单变量上提升0.6%，多变量上1.3%，证明多尺度对复杂时序更有效。
  - 跨变量注意力（多变量任务）提升VUS-PR约0.9%，Point-F1约0.8%。
  - 排序预任务替代简单邻接分类，单变量提升1.8%，多变量提升3.2%，表明恢复全局时序顺序提供更强监督。
- **轻量优势**：参数仅1.1M/1.5M，远小于大模型（MOMENT 109.6M, CATCH 210.8M），推理速度适中，适合边缘部署。

### 7. 优点
- **轻量高效**：设计紧凑，参数量比Transformer方法少1–2个数量级，适合资源受限的实时推理终端。
- **多尺度覆盖**：三分支卷积配合自适应注意力，同时捕获点突变与持续异常，提升特征丰富度。
- **跨变量建模**：显式用自注意力优化变量间依赖，弥补了原PaAno独立编码的缺陷，对交互型异常更敏感。
- **自监督预任务创新**：窗口时序排序任务强制模型学习全局时间演化规律，监督信号强于简单邻接分类。
- **全面公正的评估**：使用无偏差的TSB-AD基准，放弃点调整指标，结果可信度高。
- **鲁棒性**：超参数敏感性实验显示性能波动小（记忆压缩至1%仍保持稳定），便于实际部署。

### 8. 不足与局限
- **多变量提升有限**：尽管VUS-ROC和AUC-ROC有提升，但VUS-PR等指标与PaAno持平甚至略低（0.42 vs 0.43），跨变量注意力在部分场景下贡献需进一步挖掘。
- **单一基准验证**：仅在TSB-AD上进行实验，未在更多工业真实数据集（如SWaT、WADI等）上验证，泛化能力有待检验。
- **在线更新机制细节不足**：虽提及基于队列的无重训更新，但敏感性实验中未深入分析不同分布漂移场景下的表现。
- **窗口排序预任务超参数**：窗口长度T和权重衰减策略对性能有影响，需手动调节，可能增加调参成本。
- **计算资源报告不完整**：未提供总训练时长、GPU内存占用等具体数字，不利于复现和算力评估。
- **对比大模型的公平性**：大模型虽精度较低但基于零样本或微调，且TSB-AD的evaluation可能未针对性优化，论文引用[6]提及有偏评估导致大模型虚高，但PaAno+的对比仍需注意协议一致性。

（完）
