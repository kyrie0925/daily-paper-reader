---
title: "Lightweight Transformer Models for On-Device Fault Detection: A Benchmark Study on Resource-Constrained Deployment"
title_zh: 面向设备端故障检测的轻量级Transformer模型：资源受限部署的基准研究
authors: Disha Patel
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24173v1"
tags: ["query:anomaly-id"]
score: 8.0
evidence: 设备故障检测基准直接涉及异常检测方法
tldr: 设备端故障检测需在资源受限硬件上平衡精度与效率。本研究在三个数据集上对比传统机器学习与轻量Transformer模型，发现传统方法在精度相当的情况下效率远高于Transformer。TinyBERT-4L是较实用的Transformer选择，INT8量化和自适应推理可进一步优化。然而，在严重类别不平衡数据上所有方法表现均不佳，揭示了现有技术的局限性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24173v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24173v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 900, \"height\": 1156, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24173v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 541, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24173v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1608, \"height\": 818, \"label\": \"Table\"}]"
motivation: 在资源受限设备上部署故障检测模型需权衡精度、延迟和模型大小，现有基准缺乏对轻量Transformer的系统评估。
method: 在NASA C-MAPSS、SECOM和UCI-PM三个数据集上，对比传统ML与DistilBERT、TinyBERT等轻量Transformer，并评估INT8量化和两阶段自适应推理。
result: "C-MAPSS上Transformer匹配传统ML（87.8% F1）但模型大100倍、延迟高9000倍；TinyBERT-4L以55MB和18ms成为最佳部署选项；自适应推理达87.6% F1和19.5ms；不平衡数据集下所有方法F1低于50%。"
conclusion: 轻量Transformer精度可替代传统ML但资源消耗大，严重类别不平衡数据下当前所有方法均失效，需进一步研究专门技术。
---

## 摘要
设备端故障检测能够在无需依赖云端的情况下实现实时诊断，但在资源受限的硬件上部署机器学习模型需要在准确率、延迟和模型大小之间进行仔细权衡。我们提出了一项基准测试，针对三个公共数据集：NASA C-MAPSS涡轮风扇退化数据集、SECOM半导体制造数据集和UCI AI4I 2020预测维护数据集，比较了传统机器学习方法（随机森林、XGBoost、支持向量机、逻辑回归）与轻量级Transformer架构（DistilBERT、TinyBERT-6L、TinyBERT-4L、MobileBERT）在二分类故障检测中的性能。我们评估了分类性能（F1分数、AUC）、模型大小和CPU推理延迟，并进一步评估了INT8动态量化和两阶段自适应推理流水线。结果表明，在分离良好的传感器数据（C-MAPSS）上，轻量级Transformer在F1为87.8%时与传统机器学习相当，但模型大小大了100倍，延迟高了9000倍。TinyBERT-4L以55 MB和18 ms CPU延迟成为最易于部署的Transformer。INT8量化在保持86.9% F1的同时将大小减少了25%。我们的自适应流水线通过量化分流模型处理97.9%的预测，仅将2.1%的预测路由至更大的专家模型，在平均延迟19.5 ms下实现了87.6%的F1。在严重不平衡的数据集（SECOM、UCI-PM）上，传统方法和Transformer方法均表现挣扎，凸显了当前方法在处理故障检测中极端类别不平衡时的根本局限性。所有代码均已公开。

## Abstract
On-device fault detection enables real-time diagnostics without cloud dependency, but deploying machine learning models on resource-constrained hardware demands careful tradeoffs between accuracy, latency, and model size. We present a benchmark comparing traditional ML methods (Random Forest, XGBoost, SVM, Logistic Regression) against lightweight transformer architectures (DistilBERT, TinyBERT-6L, TinyBERT-4L, MobileBERT) for binary fault detection across three public datasets: NASA C-MAPSS turbofan degradation, SECOM semiconductor manufacturing, and UCI AI4I 2020 predictive maintenance. We evaluate classification performance (F1-score, AUC), model size, and CPU inference latency, and further assess INT8 dynamic quantization and a two-stage adaptive inference pipeline. Our results reveal that on well-separated sensor data (C-MAPSS), lightweight transformers match traditional ML at 87.8% F1 but at 100x the model size and 9000x the latency. TinyBERT-4L emerges as the most deployment-friendly transformer at 55 MB and 18 ms CPU latency. INT8 quantization reduces size by 25% while preserving 86.9% F1. Our adaptive pipeline, routing 97.9% of predictions through a quantized triage model and only 2.1% to a larger expert, achieves 87.6% F1 at 19.5 ms average latency. On severely imbalanced datasets (SECOM, UCI-PM), both traditional and transformer methods struggle significantly, highlighting fundamental limitations of current approaches for extreme class imbalance in fault detection. All code is publicly available.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：设备端故障检测需要在资源受限的硬件上实现实时诊断，部署机器学习模型时需平衡准确率、延迟和模型大小。传统云处理方式存在网络延迟、连接需求和隐私问题。现代Transformer模型虽精度高但资源消耗大，而压缩后的轻量级Transformer（如DistilBERT、TinyBERT、MobileBERT）在NLP任务上表现良好，但其在**表格型故障检测任务中的适用性**尚未得到系统评估。
- **研究动机**：填补现有基准的空白，提供一份**诚实、可复现的基准**，对比传统机器学习（ML）与轻量Transformer在真实故障检测数据集上的表现，重点关注模型大小、CPU推理延迟以及压缩技术（量化和自适应推理）的效果。
- **整体含义**：为资源受限环境下的部署提供实用指南，并指出当前方法在严重类别不平衡数据上的根本局限性。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：通过系统基准测试，评估不同复杂度模型在设备端故障检测中的权衡，并探索压缩和自适应推理以优化资源利用。
- **关键技术细节**：
  - **模型配置**：
    - **传统ML基线**：随机森林（200棵树，最大深度20）、XGBoost（200轮，最大深度6，学习率0.1）、SVM（RBF核，平衡类权重）、逻辑回归（L2正则化）。
    - **轻量Transformer**：DistilBERT（66.9M参数，6层，768隐藏维度）、TinyBERT-6L（67.0M，6层，768维）、TinyBERT-4L（14.3M，4层，312维）、MobileBERT（24.6M，瓶颈架构）。所有Transformer使用预训练权重并微调。
  - **输入表示**：将表格传感器数据**序列化为文本**（“feature_name:value feature_name:value ...”形式，最多20个特征，截断至128个token），以利用预训练语言表示。
  - **压缩技术**：
    - **INT8动态量化**：对微调后的DistilBERT和TinyBERT-4L应用PyTorch动态量化（量化所有Linear层）。
    - **两阶段自适应推理流水线**：Stage 1为量化TinyBERT-4L分流模型，若最大类别概率≥阈值τ则接受预测；Stage 2将低置信度样本路由至全精度DistilBERT专家模型。τ从{0.6,0.65,0.7,0.75,0.8,0.85,0.9,0.95}中选取使F1最大的值。
  - **微调细节**：初始训练5个epoch，后续使用加权交叉熵再训练7-8个epoch，学习率2e-5至5e-5，批次大小32，warmup步数100，在NVIDIA T4 GPU上进行。
- **算法流程（文字说明）**：先微调Transformer → 对部分模型进行INT8量化 → 构建自适应流水线（先经分流模型，低置信度再经专家模型）→ 在所有数据集上统一评估分类性能和效率。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**（三个公共故障检测数据集，均按80/20分层划分）：
  1. **NASA C-MAPSS**：涡扇发动机退化仿真，24个特征，20,631样本，二分类（30周期内故障），故障率15.0%。
  2. **SECOM**：半导体制造数据，562个特征（去除缺失>50%的列），1,567样本，故障率6.6%。
  3. **UCI AI4I 2020**：预测维护数据集，8个特征（5个传感器+3个one-hot编码），10,000样本，故障率3.4%。
- **基准**：无特定外部基准，论文自身为系统基准。
- **对比方法**：
  - 传统ML：RF-200、XGBoost、SVM、LR
  - 轻量Transformer（FP32）：DistilBERT、TinyBERT-6L、TinyBERT-4L、MobileBERT
  - 使用加权损失的Transformer（再训练版本）：DistilBERT w、TinyBERT-6L w、TinyBERT-4L w
  - 量化版本：DistilBERT+INT8、TinyBERT-4L+INT8
  - 自适应流水线：TinyBERT-4L (INT8)分流 + DistilBERT专家
- **评价指标**：F1-score（主要）、AUC、模型大小（MB）、CPU推理延迟（单样本平均，50-100次运行预热后，在T4 GPU和Colab CPU上测量）。

## 4. 资源与算力
- **文中明确说明**：
  - 所有Transformer微调在**1块NVIDIA T4 GPU**上进行，训练5-8个epoch，使用批量大小32。
  - CPU延迟测量在**Colab CPU**（具体型号未说明）上进行。
  - 未给出总训练时间或GPU总使用时长。
  - **注意**：文中指出CPU延迟测量非专用移动硬件（如Apple Neural Engine或Qualcomm NPU），可能迁移性有限。

## 5. 实验数量与充分性
- **实验数量**：
  - 3个数据集 × 10种模型配置（4传统+4Transformer+2加权损失+2量化+1自适应流水线，但加权损失和量化与FP32版本重叠，总计约15-16种不同配置，每个数据集均报告）。
  - 自适应流水线对τ进行超参数搜索（8个值）。
  - MobileBERT失败分析有额外验证（不同学习率、epoch）。
  - 压缩-准确率帕累托分析（图1）。
- **充分性与公平性**：
  - **优点**：覆盖传统ML与多种Transformer变体，包含量化、加权损失和自适应方法；使用统一标准评估；公开代码；诚实地报告了MobileBERT完全失败和所有方法在严重不平衡数据集上的低性能。
  - **不足**：仅使用数据集80/20一次划分（随机种子42），无交叉验证或多次重复实验以评估稳定性；SECOM和UCI-PM故障率极低，未采用重采样或代价敏感学习（仅加权损失），可能导致对Transformer不公平；未与其他领域专门的时间序列Transformer（如Autoformer）对比；非线性Transformer架构（MobileBERT）的失败可能受限于输入表示（文本序列化），未尝试直接数值嵌入。

## 6. 论文的主要结论与发现
- **Finding 1**：在分离良好的数据（C-MAPSS）上，轻量Transformer精度匹配传统ML（如TinyBERT-6L 87.9% F1 vs XGBoost 87.9% F1），但**模型大小大510倍**、**延迟高66500倍**。传统ML仍是实际选择。
- **Finding 2**：TinyBERT-4L是最适合部署的Transformer（14.3M参数，55 MB，18 ms CPU延迟），F1达87.8%。
- **Finding 3**：INT8量化在保持大部分精度的前提下减少模型大小（TinyBERT-4L+INT8: 41 MB，25%缩减，F1 86.9%）。
- **Finding 4**：自适应流水线（分流+专家）以19.5 ms平均延迟实现87.6% F1，其中97.9%样本由轻量分流模型处理。
- **Finding 5**：极端类别不平衡（SECOM、UCI-PM）使所有方法失败，最好F1仅13.6%（SECOM上LR），XGBoost在UCI-PM上达到83.3%（但对Transformer仍仅50%）。类别不平衡是当前技术的**主要瓶颈**而非模型架构。
- **MobileBERT完全失败**：在所有数据集上预测多数类（F1=0%），归因于其倒瓶颈架构不适合序列化数值数据。

## 7. 优点
- **诚实透明**：明确报告失败案例（MobileBERT全0、不平衡数据低F1），不遮掩负面结果。
- **实用性导向**：关注具体部署指标（模型大小、CPU延迟、INT8量化、自适应流水线），给出分场景的部署建议（<1MB预算用XGBoost/LR；50-100MB用TinyBERT-4L+INT8；>100MB用自适应流水线）。
- **全面评估**：涵盖传统ML、多代轻量Transformer、量化、加权损失和新型自适应推理，对每个配置均报告多维度指标。
- **可复现性**：代码公开；使用标准公开数据集；详细说明训练超参和评估协议。
- **问题洞察**：通过帕累托前沿分析（图1）清晰展示效率边界；指出Transformer在表格数据上资源浪费的现象。

## 8. 不足与局限
- **实验覆盖**：
  - **CPU延迟测量环境**：在Colab CPU上测量，未使用专用移动硬件（如NPU），实际设备性能可能不同。
  - **仅一次数据划分**：无交叉验证或多次重复实验，可能结果不稳定（尤其小数据集SECOM仅1,567样本）。
  - **未探索其他压缩技术**：如剪枝、量化感知训练（QAT），可能进一步提高量化模型精度。
  - **未评估推理吞吐量（batch处理）**：仅报告单样本延迟，批处理场景可能不同。
- **方法局限性**：
  - **输入表示**：转为文本增加token化开销和潜在信息丢失，混淆了对Transformer架构本身在图数据上性能的评估；MobileBERT失败可能部分源于此，若能直接处理数值嵌入或许不同。
  - **不平衡处理**：仅用加权损失和平衡类权重，未使用SMOTE、Focal Loss、代价敏感学习或集成方法，对不平衡数据的评估不够充分。
- **结论外推性**：
  - 结论仅适用于表格型传感器数据，不适用于其他模态（如语音、振动时序）。
  - C-MAPSS数据已较干净；真实工业数据可能存在噪声、缺失、多模态，本基准未覆盖。
- **客观性风险**：
  - 所有实验由单一作者执行，无独立验证。
  - 自适应流水线的阈值通过测试集最大化F1选择，可能过拟合测试集。

（完）
