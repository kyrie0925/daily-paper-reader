---
title: "HiMatch-AD: DINOv3-driven Hierarchical Matching for Training-free Medical Anomaly Detection"
title_zh: "HiMatch-AD: DINOv3驱动的分层匹配用于免训练医学异常检测"
authors: "Jiayu Huo, Jingyuan Hong, Meng Zhou, Liyun Chen, Le Zhang"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22556v1"
tags: ["query:anomaly-id"]
score: 8.0
evidence: 无需训练的医学异常检测方法，使用DINOv3
tldr: 医学图像异常检测中，基于训练的方法因任务优化和大量正常数据需求而扩展性差。现有训练无关方法采用简单最近邻检索和聚合，无法捕获分层语义且忽略多异常响应可靠性。本文提出HiMatch-AD，利用DINOv3进行双分支检索获取语义相关正常参考，跨Transformer阶段生成分层异常图，并通过不确定性融合机制自适应加权多级响应。在脑MRI、肝CT和视网膜OCT基准上，该方法超越现有训练基和DINO基方法，无需任务特定训练即实现可扩展的异常检测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22556v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1232, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22556v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1246, \"height\": 356, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22556v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 544, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22556v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 955, \"height\": 515, \"label\": \"Table\"}]"
motivation: 现有训练无关异常检测方法未能充分利用预训练模型的分层语义，简单聚合策略也忽略了多异常响应的可靠性问题。
method: HiMatch-AD先通过双分支检索获取语义相关正常参考，再跨多个Transformer阶段生成分层异常图，最后用不确定性融合机制自适应加权。
result: 在BMAD基准（脑MRI、肝CT、视网膜OCT）上，HiMatch-AD一致超越训练基和DINO基方法，取得最优性能。
conclusion: 多级匹配与不确定性融合有效提升了可扩展医学异常检测性能，无需任务特定训练。
---

## 摘要
异常检测对于医学图像分析至关重要，病理区域通常表现为与正常解剖结构的罕见偏差。基于训练的方法虽然取得了有希望的性能，但需要任务特定的优化和大量的正常数据，这限制了跨模态和机构的可扩展性。免训练方法通过利用预训练的视觉表示提供了更大的灵活性，但现有方法通常依赖于简单的最近邻检索和朴素聚合策略，可能无法捕捉分层语义，并忽略多个异常响应的可靠性。在这项工作中，我们提出了HiMatch-AD，一种DINOv3驱动的分层匹配框架，用于免训练医学异常检测。我们的方法首先通过双分支匹配检索语义相关的正常参考，该匹配同时考虑全局CLS令牌相似性和补丁级表示。然后通过将聚类后的正常特征与查询表示进行比较，在多个变换器阶段生成分层异常图。为了稳健地聚合异常响应，我们引入了一种基于统一不确定性的融合机制，该机制根据图的可靠性自适应地加权。整个框架无需任何任务特定训练。在BMAD基准（包括脑MRI、肝脏CT和视网膜OCT数据集）上的广泛实验表明，HiMatch-AD始终优于基于训练和基于DINO的最新方法，这突显了多层次匹配和不确定性感知融合对于可扩展医学异常检测的有效性。

## Abstract
Anomaly detection is essential for medical image analysis, where pathological regions often appear as rare deviations from normal anatomical structures. While training-based methods have achieved promising performance, they require task-specific optimization and extensive normal data, which limits scalability across modalities and institutions. Training-free approaches offer greater flexibility by leveraging pretrained visual representations, yet existing methods typically rely on simple nearest-neighbor retrieval and naive aggregation strategies, which may fail to capture hierarchical semantics and ignore the reliability of multiple anomaly responses. In this work, we propose HiMatch-AD, a DINOv3-driven hierarchical matching framework for training-free medical anomaly detection. Our method first retrieves semantically relevant normal references via dual-branch matching that jointly considers global CLS-token similarity and patch-level representations. Hierarchical anomaly maps are then generated across multiple transformer stages by comparing clustered normal features with query representations. To robustly aggregate anomaly responses, we introduce a unified uncertainty-based fusion mechanism that adaptively weights maps according to their reliability. The entire framework operates without any task-specific training. Extensive experiments on the BMAD benchmark, including brain MRI, liver CT, and retinal OCT datasets, demonstrate that HiMatch-AD consistently outperforms both training-based and DINO-based state-of-the-art methods, which highlights the effectiveness of multi-level matching and uncertainty-aware fusion for scalable medical anomaly detection.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：医学图像异常检测中，病理区域通常表现为与正常解剖结构的罕见偏差。现有方法分为基于训练和免训练两类。基于训练的方法（如师生网络、重构模型）需要任务特定优化和大量正常数据，可扩展性差；免训练方法虽利用预训练视觉表示，但多采用简单最近邻检索和朴素聚合策略，无法捕获分层语义，且忽略多个异常响应的可靠性差异。
- **整体含义**：本文提出一种**无需任务特定训练**的医学异常检测框架，通过利用DINOv3的强大预训练表示，结合分层匹配和不确定性感知融合，实现跨模态、跨机构的可扩展异常检测。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：基于DINOv3（ViT-B/16）的**分层匹配**，包括：双分支支持图像检索、跨Transformer阶段分层异常图生成、统一不确定性融合。
- **关键技术细节**：
  - **双分支支持图像检索**：对查询图像和正常图像池分别提取CLS令牌和补丁令牌。对补丁令牌做空间平均池化得到全局补丁描述符。计算CLS分支和补丁分支的余弦相似度，加权求和后选择Top-K正常图像作为支持集。
  - **分层异常图生成**：将DINOv3的12个Transformer块每3个分为一个阶段（共4个阶段）。对每个阶段，提取查询和支持图像的补丁特征。对每个支持图像的阶段特征执行K-means聚类（C=2个中心），得到代表正常分布的中心点。计算每个中心与查询特征每个补丁的余弦相似度，转换为异常响应（1 - 归一化相似度）。通过softmax融合多个中心的异常响应，得到该支持图像在该阶段的异常图。
  - **不确定性融合**：先对同一阶段的多个支持图像异常图进行实例融合：计算均值图，然后计算每个图与均值的偏差作为不确定性，用softmin得到权重，加权融合得到阶段融合图。再对4个阶段融合图做同样的过程（阶段融合），得到最终异常图。
- **公式/算法流程**（文字说明）：
  - 检索阶段：\( s = \text{sim}_{\text{cls}} + \text{sim}_{\text{patch}} \) → Top-K。
  - 生成阶段：对每个支持图像每阶段 → K-means聚类 → 余弦相似度 → 异常响应 → softmax融合 → 单个异常图。
  - 融合阶段：实例融合（基于不确定性权重） → 阶段融合（相同流程）。

## 3. 实验设计
- **数据集**：使用BMAD基准中的三个医学影像数据集：
  - **脑MRI**：BraTS 2021衍生，训练集7500正常图像，测试集640正常+3075异常（有像素级标注）。
  - **肝脏CT**：BTCV和LiTs衍生，训练集1542正常，测试集833正常+660异常（有分割掩码）。
  - **视网膜OCT**：RESC数据集，训练集4297正常，测试集1041正常+764异常（有掩码）。
- **Benchmark**：BMAD（医学异常检测基准）。
- **对比方法**：包括训练基方法（DRAEM, UTRAD, SimpleNet, MKD, RD4AD, STFPM, PaDiM）和免训练/记忆基方法（PatchCore, AnomalyDINO, DINO-DPMM）。共13种方法。
- **评估指标**：图像级AUC（I-AUC）、像素级AUC（P-AUC）、像素级Per-Region Overlap（P-PRO）。

## 4. 资源与算力
- **文中未明确说明**所使用的GPU型号、数量、训练时长等算力信息。仅提到所有实验基于DINOv3 ViT-B/16，参数冻结，无任务特定训练。在消融实验中给出了支持集大小K对推理速度的影响（从K=1到16的每秒查询数），但未详细描述硬件配置。因此无法确定具体算力消耗。

## 5. 实验数量与充分性
- **实验数量**：
  - 主要对比实验：在3个数据集上，与13种方法进行3项指标（共9组比较）的全面对比。
  - 消融实验：
    - 不同ViT骨干（CLIP-ViT, DINOv2, DINOv3）在3个数据集上的P-AUC比较。
    - 支持集大小K（1,4,8,16）在脑MRI上的P-AUC和推理速度对比。
    - 匹配策略消融：无匹配、仅CLS、仅Patch、两者结合，在3个数据集上的P-AUC。
    - 融合策略消融：无融合（平均）、仅阶段融合、仅实例融合、两者结合，在3个数据集上的P-AUC。
  - 可视化展示：图2右侧展示3个数据集的异常图与GT对比。
- **充分性与公平性**：
  - 对比方法覆盖全面，包括近期SOTA（AnomalyDINO等）。
  - 消融实验系统，逐一验证各组件（骨干、支持集大小、匹配策略、融合策略）的贡献。
  - 所有实验在相同数据集和指标下进行，公平性较好。
  - 不足之处：消融实验中对支持集大小K的性能影响仅在脑MRI上测试，未在肝脏CT和视网膜OCT上重复，可能削弱泛化结论；且未对K-means聚类数C进行消融（仅用2）。

## 6. 主要结论与发现
- HiMatch-AD在三个数据集上均**超越所有对比方法**，包括训练基和免训练方法。
- 具体性能：脑MRI上I-AUC 92.91%、P-AUC 98.87%、P-PRO 87.73%；肝脏CT上77.84%、98.92%、92.75%；视网膜OCT上92.61%、97.01%、86.23%。
- DINOv3优于DINOv2和CLIP-ViT作为骨干。
- 双分支匹配（CLS+补丁）显著优于单分支或随机检索。
- 不确定性融合（实例+阶段联合）优于平均融合或单一融合。
- 支持集K=4是性能与速度的良好平衡点。
- 无需任务特定训练即可实现SOTA，表明预训练表示+合理匹配聚合可替代训练基方法。

## 7. 优点：方法或实验设计上的亮点
- **方法创新性**：
  - 首次将DINOv3用于免训练医学异常检测，利用其分层特征（多个Transformer阶段）。
  - 双分支检索同时考虑全局和局部语义。
  - 基于聚类的正常分布建模（K-means）提供更鲁棒的代表性。
  - 不确定性融合机制自适应加权多源异常响应，避免简单平均的退化。
- **实验设计**：
  - 在标准化基准BMAD上评估，对比方法全面（13种），涵盖训练基和免训练。
  - 消融实验系统，验证各设计选择。
  - 可视化结果直观展示定位能力。
  - 使用P-PRO指标（Per-Region Overlap）更公平地评估区域级定位。

## 8. 不足与局限
- **实验覆盖不足**：
  - 仅在三种模态（MRI, CT, OCT）上评估，缺乏对X光、超声等其他模态的验证，应用泛化性未充分证明。
  - 未在自然图像异常检测基准（如MVTec AD）上测试，影响方法通用性。
  - 缺少数值稳定性和超参数敏感性分析（如聚类数C=2是否最优）。
- **偏差风险**：
  - 支持集检索依赖正常图像池，若正常池包含潜在异常或分布偏移，检索可能失败。
  - K-means聚类假设各向同性高斯簇，可能不适用于复杂正常分布。
  - 不确定性融合基于均方误差，对极端异常响应可能不够鲁棒。
- **应用限制**：
  - 推理速度随支持集大小增加显著下降（K=16时仅0.64 qps），实时性受限。
  - 需要预训练DINOv3模型，模型较大，部署成本较高。
- **算力信息缺失**：未报告实验环境，影响可复现性评估。
- **理论分析不足**：未从理论上解释不确定性融合的有效性，仅凭实验验证。

（完）
